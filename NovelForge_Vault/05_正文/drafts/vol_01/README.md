# 正文草稿目录（drafts）

> 本目录存放 writer Skill 生成的草稿，按卷分片。
> 文件命名：`ch_NNN_标题.md`，例如 `ch_001_觉醒.md`。
> polisher 修订完成且 state_update 落地后，草稿发布由主 Agent 操作：将文件从 `drafts/vol_NN/` 移到 `published/vol_NN/`，并更新 frontmatter 的 `status` 字段为 `published`。

## vol_01

（草稿文件将在此生成）
